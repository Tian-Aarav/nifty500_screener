services:
  - type: web
    name: nifty500-screener
    env: python
    runtime: python-3.10.13
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run nifty500_screener/main.py
